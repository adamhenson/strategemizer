import sAndP500 from './public/sAndP500';

// import somePrivateList from './private/somePrivateList';

type Symbols = Record<string, string[]>;
const symbols: Symbols = {
  sAndP500,
  // somePrivateList,
};

export default symbols;
